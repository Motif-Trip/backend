<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.motif.app.domain.mapper.LikeMapper">
	<insert id="like" parameterType="Like" useGeneratedKeys="true" keyProperty="likeId">
		INSERT INTO Likes(post_id, member_id)
		VALUES(#{postId}, (SELECT member_id FROM Members WHERE email=#{email}))
	</insert>

	<select id="isLiked" parameterType="Like" resultType="Long">
		SELECT like_id
		FROM Likes
		WHERE member_id=(SELECT member_id FROM Members WHERE email=#{email})
	</select>
	
	<delete id="unlike" parameterType="Long">
		delete from Likes
		where like_id=#{likeId}
	</delete>
	
	<select id="likeCount" resultType="int">
		select count(*)
		from Likes
		where post_id=#{postId}
	</select>
</mapper>